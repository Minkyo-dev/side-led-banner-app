import type { ImageSourcePropType } from "react-native";

/**
 * `app.json`의 `expo.icon`과 동일한 파일.
 * 리워드 모달 등에서 앱 썸네일로 사용합니다.
 */
export const APP_EXPO_ICON: ImageSourcePropType = require("../assets/images/splash-icon.png");
